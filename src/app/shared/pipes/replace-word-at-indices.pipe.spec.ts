import { ReplaceWordAtIndicesPipe } from './replace-word-at-indices.pipe';

describe('ReplaceWordAtIndicesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWordAtIndicesPipe();
    expect(pipe).toBeTruthy();
  });
});
